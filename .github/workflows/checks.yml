name: Runs SCRAM static checks

on:
  workflow_call:
    inputs:
      pr-number:
        description: PR number
        required: true
        type: number
      required-prs:
        description: Comma-separated list of required PR numbers that must be merged before checks are run
        required: false
        type: string


jobs:
  checks:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: echo "Test"
